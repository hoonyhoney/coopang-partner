server:
  port: 8080

spring:
  application:
    name: coopang-

  datasource:
    url: jdbc:mariadb://localhost:3306/coopang
    username: root
    password: donghun1986
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1

